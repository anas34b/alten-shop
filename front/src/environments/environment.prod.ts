export const environment = {
  production: true,
  // En PROD, le front et le back sont sur des domaines differents (pas de proxy).
  // URL du back deploye sur Render :
  apiUrl: "https://alten-shop-back-33jv.onrender.com",
};
